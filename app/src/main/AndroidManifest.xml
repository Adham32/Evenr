<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anglehack.eventr">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".Activities.TitleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.GameActivity"
            android:label="Coca-cola Pong"
            android:theme="@style/AppThemeSnd" />
        <activity
            android:name=".Activities.HelpActivity"
            android:label="Help"
            android:theme="@style/AppThemeSnd" />
        <activity android:name=".Activities.EnterTicker" />
        <activity android:name=".Activities.FoodDetails"
            android:label="KFC"
            android:theme="@style/AppThemeSnd"></activity>
    </application>

</manifest>